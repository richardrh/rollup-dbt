version: 2

models:
  - name: stg_reference__forecast_factors
    description: "Staged forecast factors with stable surrogate key."
    columns:
      - name: forecast_factor_id
      - name: modelled_lob
      - name: forecast_date
      - name: factor
      - name: office
      - name: class
