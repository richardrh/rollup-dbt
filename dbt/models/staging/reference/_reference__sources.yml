version: 2

sources:
  - name: reference
    description: "Reference datasets naming the lookup views we just built"
    schema: cat_models
    tables:
      - name: lobs
        description: "HISCO LOB metadata plus type labels."
        identifier: stg_reference__hisco_org__lobs
      - name: offices
        description: "Office lookup metadata."
        identifier: stg_reference__hisco_org__offices
