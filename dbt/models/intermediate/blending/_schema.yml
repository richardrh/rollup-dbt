version: 2

models:
  - name: int_blending__010_funnel_ep
    description: "Funnel that brings together EPs into one matched table"
    config:
      schema: blending
      materialized: view
    columns:
      - name: event_id
      - name: rate
      - name: perspcode
      - name: meanloss
      - name: stddevi
      - name: stddevc
      - name: exp_value
