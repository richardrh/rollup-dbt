version: 2

seeds:
  - name: forecast_factors
    description: "Forecast factor registry by LOB and forecast date."
    config:
      column_types:
        modelled_lob: varchar
        forecast_date: date
        office: varchar
        class: varchar
        factor: float
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns: [modelled_lob, forecast_date, office, class]
    columns:
      - name: modelled_lob
        tests:
          - not_null
          - relationships:
              arguments:
                to: ref('hisco_org__lobs')
                field: modelled_lob
      - name: forecast_date
        tests:
          - not_null
      - name: factor
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0.0
                max_value: 10.0
          - expression_is_true:
              arguments:
                expression: "factor > 0"
      - name: office
        tests:
          - dbt_utils.not_empty_string
      - name: class
        tests:
          - dbt_utils.not_empty_string
